# Superseded. The single source of truth is the repository-root requirements.txt.
#
# This file used to be a second, independently-drifting copy. It disagreed with
# the root file on CairoSVG (2.4.2 vs 2.7.0), Pillow (8.0.1 vs 10.3.0), patsy and
# statsmodels, so which versions you got depended on which file you happened to
# install from.
-r ../../requirements.txt
